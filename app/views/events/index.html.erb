<h1>Termine</h1>

<table>
  <tr>
    <th>Beginn</th>
    <th>Ort</th>
    <th>Orchester</th>
    <th>Veranstaltung</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= l event.begin, :format=>:long %></td>
    <td><%= event.location %></td>
    <td><%= event.orchestra %></td>
    <td><%= event.title %></td>
    <td><%= link_to 'Details', event %></td>
    <td><%= link_to('Bearbeiten', edit_polymorphic_path(event)) if user_signed_in?  %></td>
    <td><%= link_to('Löschen', event, :confirm => 'Soll der Termin wirklich gelöscht werden?', :method => :delete) if user_signed_in? %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to( 'Neuer Termin', new_event_path) if user_signed_in? %>
<%= link_to( 'Neues Konzert', new_concert_path ) if user_signed_in? %>

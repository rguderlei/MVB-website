<%= simple_form_for(@info, :html => {:class => 'form-horizontal' }) do |f| %>
    <%= f.input :title, :label=>'Überschrift' %>
    <%= f.input :content, :label=>'Text', :as=>:text, :input_html => { :cols => 200, :rows => 5, :class=>'span6' }%>
    <%= f.button :submit, 'Speichern', :class=>'btn primary' %>
<% end %>

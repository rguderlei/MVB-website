<h1>Akutelles</h1>

<table>
  <tr>
    <th>Titel</th>
    <th>Datum</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @infos.each do |info| %>
      <tr>
        <td><%= info.title %></td>
        <td><%= l info.created_at, :format=>:short %></td>
        <td><%= link_to 'Anzeigen', info %></td>
        <td><%= link_to 'Bearbeiten', edit_info_path(info) if user_signed_in? %></td>
        <td><%= link_to 'Löschen', info, :confirm => 'Sicher?', :method => :delete if user_signed_in? %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'Neue Info anlegen', new_info_path if user_signed_in? %>

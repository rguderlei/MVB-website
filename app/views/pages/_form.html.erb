<%= simple_form_for(@page) do |f| %>
    <%= f.input :name %>
    <%= f.input :permalink %>
    <%= f.ckeditor :main_content, :label => false %>
    <%= f.button :submit, 'Speichern', :class=>'btn primary'%>
<% end %>

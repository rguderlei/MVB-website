<%= simple_form_for(@page, :html => {:class => 'form-horizontal' }) do |f| %>
    <div class="row">
        <div class="span12">
          <%= f.input :name %>
          <%= f.input :permalink %>
          <%= f.cktext_area :main_content, :label => false %>
        </div>
    </div>

    <div class="row">
      <div class="span4">
        &nbsp;
      </div>
    </div>


    <div class="row">
      <div class="span4">
        <%= f.button :submit, 'Speichern', :class=>'btn btn-danger'%>
      </div>
    </div>


<% end %>

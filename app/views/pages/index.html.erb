<h3>Bearbeitbare Seiten</h3>
<section>
  <article class="col-1">
    <%= link_to('Neue Seite anlegen', new_page_path) if user_signed_in? %>
  </article>
</section>

<section>
  <article class="col-1">
    <table class="table table-striped">
      <tr>
        <th>Name</th>
        <th>Permalink</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <% @pages.each do |page| %>
          <tr>
            <td><%= page.name %></td>
            <td><%= page.permalink %></td>
            <td><%= link_to 'Anzeigen', page %></td>
            <td><%= link_to('Bearbeiten', edit_page_path(page)) if user_signed_in? %></td>
            <td><%= link_to('Löschen', page, data: { confirm: "Soll die Seite wirklich gelöscht werden?" }, :method => :delete) if user_signed_in? %></td>
          </tr>
      <% end %>
    </table>
  </article>
</section>




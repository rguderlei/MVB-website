<div class="page-header">
    <h2><%= @page.name %></h2>
</div>

<%= @page.main_content.html_safe %>

<% if user_signed_in? then %>
    <br />
    <%= link_to('Bearbeiten', edit_page_path(@page)) %> | <%= link_to('Zurück zur Übersicht', pages_path) %>
<% end %>


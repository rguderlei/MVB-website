<%= simple_form_for(@user) do |f| %>
    <%= f.input :email, :label=>'Email-Addresse'  %>
    <%= f.input :password, :as=>:password, :label=>'Passwort'  %>
    <%= f.input :password_confirmation, :as=>:password, :label=>'Passwort-Bestätigung'  %>
    <%= f.input :admin, :label=>'Administrator?'  %>

    <%= f.button :submit, 'Speichern', :class=>'btn primary' %>
<% end %>
